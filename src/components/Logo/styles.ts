import styled from "styled-components";

export const Container = styled.div`
  padding-top: 15px;
  display: flex;
  svg {
    fill: var(--shape);
    font-size: 3rem;
    position: relative;
    top: -10px;
  }
`;

export const Title = styled.h2`
  color: var(--shape);
`;
